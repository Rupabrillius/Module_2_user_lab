{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Payments Domain Payload",
  "type": "object",
  "required": ["payment_id", "order_id", "status", "latency_ms"],
  "properties": {
    "payment_id": { "type": "string" },
    "order_id": { "type": "string" },
    "status": { "type": "string", "enum": ["started", "authorized", "failed"] },
    "latency_ms": { "type": "number", "minimum": 0 }
  },
  "additionalProperties": true
}
